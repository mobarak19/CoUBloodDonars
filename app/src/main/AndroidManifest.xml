<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cse.cou.mobarak.coublooddonars">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserRegistration" />
        <activity android:name=".BloodDonars" />
        <activity android:name=".UpdateAccount" />
        <activity android:name=".DonarsDetails" />
        <activity android:name=".UpdataUserAgain" />
        <activity android:name=".Developers" />
        <activity android:name=".DevelopersFb"></activity>
    </application>

</manifest>